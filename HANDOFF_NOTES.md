# TMS (TFAM Management System) — Handoff Notes

Written 2026-09-09 for continuity to another AI assistant / developer picking up this work.

## What this is

A claims/survey job tracker built for TFAM, an insurance loss-assessor/surveyor firm. It's a Kanban-style board of insurance claim jobs moving through a pipeline (New Claim → Awaiting Inspection → ... → Closed), with staff assignment, document checklists, an activity log, and now real login/auth.

## Live deployment

- **Live app**: https://tms-worker.tfamisla.workers.dev
- **GitHub repo**: https://github.com/tfamisla/tms.git (branch `main`)
- **Cloudflare account**: tfamisla@gmail.com
- **GitHub account**: tfamisla (auth via `gh auth login`, browser flow — do NOT try username/password git push, GitHub rejects it; use the `gh` CLI's credential helper, already configured on this machine)
- Local project path: `/Users/rajanaren/Documents/TMS`

### Architecture — single Worker serves everything

There is **no separate Cloudflare Pages site**. One Cloudflare Worker (`worker.js`) does two jobs:
1. Serves the static frontend (`public/index.html`) via Wrangler's `[assets]` config in `wrangler.toml` (directory `./public`, binding `ASSETS`)
2. Handles the JSON API under `/api/*`

This was a deliberate choice (see git log) instead of the classic "separate Pages + Workers" split, because Cloudflare's dashboard "Connect to Git" flow auto-detected `wrangler.toml` and defaulted to a Workers Git integration anyway. So `index.html` and the API are same-origin — the frontend's `API` const is just `''` (empty string), all fetches are relative paths like `/api/jobs`.

**Cloudflare Git integration**: the repo is connected to Cloudflare's Workers Builds — every push to `main` on GitHub triggers `npx wrangler deploy` automatically on Cloudflare's side. In addition, deploys have also been run manually from this machine via `wrangler deploy` throughout development. Both point at the same Worker, so they're redundant, not conflicting.

### Database — Cloudflare D1

- Database name: `TMS_DB`, id `bde9756d-49c9-45d9-b4f9-21420b904a32`, bound as `TMS_DB` in `wrangler.toml`
- **`schema.sql`** is the fresh-install source of truth — every statement uses `CREATE TABLE IF NOT EXISTS` / `INSERT OR IGNORE`, safe to re-run
- **`migrations/*.sql`** are one-time, non-idempotent scripts (mostly `ALTER TABLE ... ADD COLUMN`) applied to the already-live database as features were added. Applied in order: `002_auth_and_acronyms.sql`, `003_job_detail_fields.sql`. If you add more schema changes, add a new numbered migration file rather than editing schema.sql's `CREATE TABLE` in a way that would break `ALTER TABLE` idempotency — and apply it with:
  ```
  wrangler d1 execute TMS_DB --remote --file=migrations/00X_name.sql
  ```

## ⚠️ Important constraint: do not touch the live database directly

The user has real employee data loaded (all placeholder names have been replaced with real staff — Aswin Joseph, Subramanian P, Ramanathan, Iyappan, Murugan C, Thendralarasi, Keethana, Magesh D, plus Raja Naren R and Raja Thiravia Kumar) and has explicitly said: **never alter the database directly once real data is in it.** Schema migrations (new columns/tables) are fine when a feature genuinely needs them, but never run ad-hoc `INSERT`/`UPDATE`/`DELETE` against real rows, and don't create throwaway test data on the live DB to verify things — verify via code review, syntax checks (`node --check worker.js`, extracting and `new Function()`-checking the inline `<script>`), and read-only `SELECT`/`PRAGMA` queries only.

## Data model (current, as of migration 003)

### `jobs`
Primary key `id` (e.g. `"TF-001"`, auto-numbered via the `counter` table, OR manually entered — see below). Columns: `title`, `insurer`, `insured`, `policy_no`, `policy_name`, `policy_period`, `claim_no`, `peril`, `claim_amount`, `estimated_loss`, `gross_loss`, `department`, `appointing_office`, `appointing_person`, `contact_person`, `contact_phone`, `address`, `district`, `date_loss`, `date_intimation`, `survey_date`, `appointment_date`, `appointment_confirmed` (0/1), `stage` (pipeline stage id), `assigned` (JSON array of staff ids — **legacy**, see "In progress" below), `notes`, `docs` (JSON object of doc-checklist flags), `activity` (JSON array of `{text, ts, actor}`), `created_at`, `updated_at`.

### `staff`
`id`, `name`, `role` (free-text job title like "Director", "Employee Surveyor" — **not** the same as `access_role`), `access_role` ('staff' or 'admin' — controls login permissions), `initials`, `color`, `password_hash` (PBKDF2, nullable until the user has a password), `created_at`.

**`rajan` (Raja Naren R) is permanently locked to `access_role = 'admin'`** — enforced server-side in `worker.js`, can never be demoted or deleted by anyone, including other admins.

### `insurers`
`id`, `name` (unique), `acronym` (up to 5 letters, auto-suggested from the name but editable), `created_at`.

### `sessions`
`token` (primary key, the session cookie value), `staff_id`, `created_at`, `expires_at` (30-day TTL).

### `counter`
Single row (`id='main'`) tracking the next auto-generated job number (`TF-XXX`).

## Auth system

- Passwords hashed with PBKDF2-SHA256 (100,000 iterations, random 16-byte salt), stored as `base64(salt):base64(hash)` in `staff.password_hash`
- Sessions are **HttpOnly, Secure, SameSite=Lax cookies** (`tms_session`), validated server-side against the `sessions` table on every request
- **Bootstrap flow**: on a fresh install, no one has a password. Only `rajan` may self-activate — his first login attempt sets whatever password he types as his permanent password. Every other staff member must be given a password by an admin (via the Staff screen's edit modal → "Reset Password" field) before they can log in at all — this prevents anyone else from claiming an account
- **Authorization**: every `/api/*` route requires a valid session except `POST /api/auth/login` and `GET /api/auth/roster` (id+name only, used to populate the login screen's dropdown, no sensitive data). Staff create/edit/delete/password-reset requires `access_role === 'admin'` (checked server-side in `worker.js`, not just hidden client-side). Any signed-in user can change their own password via `POST /api/auth/change-password`
- Client-side: `public/index.html` shows a full-screen login overlay until `GET /api/auth/me` succeeds; a 401 from any API call anywhere in the app triggers `sessionExpired()` which re-shows the login screen

## API endpoints (all in `worker.js`)

```
GET    /api/auth/roster              — public, {id,name} list for login dropdown
POST   /api/auth/login               — public, body {id, password} → sets session cookie
GET    /api/auth/me                  — current session's staff record
POST   /api/auth/logout              — clears session
POST   /api/auth/change-password     — body {oldPassword, newPassword}

GET    /api/jobs                     — list all jobs
POST   /api/jobs                     — create; body.id optional (manual numbering), else auto TF-XXX
PATCH  /api/jobs/:id                 — partial update; body.activity_add appends to the activity log

GET    /api/log                      — flattened activity feed across all jobs (latest 500)

GET    /api/staff                    — list (password_hash never included in responses)
POST   /api/staff                    — admin only
PATCH  /api/staff/:id                — admin only; body.access_role and body.password both gated
                                        server-side (rajan's access_role can't be changed)
DELETE /api/staff/:id                — admin only; rajan cannot be deleted

GET    /api/insurers                 — list
POST   /api/insurers                 — any signed-in user; dedupes case-insensitively by name
PATCH  /api/insurers/:id             — any signed-in user
DELETE /api/insurers/:id             — any signed-in user
```

## Frontend structure (`public/index.html` — single file, ~1400 lines)

- Inline `<style>` (CSS custom properties for a light/dark navy+amber theme), inline `<script>`, no build step, no framework
- Views: Dashboard (Kanban board), Staff (team grid + insurance companies list), Work Log (activity feed with filters)
- Modals: New Job, New/Edit Staff (admin), New/Edit Insurer, Change My Password, plus a slide-out side panel for job detail/edit
- Key state variables: `jobs`, `log`, `TEAM` (staff, was `const` originally, now mutable and API-driven), `INSURERS`, `currentUser`/`currentUserRecord` (from session), `panelState` (pending edits in the open job's side panel before Save)
- `fetchAll()` polls every 5 seconds while logged in (`pollTimer`)

## Git / deploy workflow used throughout this project

```bash
# Local dev tools already installed on this Mac Mini:
wrangler --version   # 4.130.0, installed globally via npm
gh auth status        # authenticated as tfamisla

# Typical change cycle:
# 1. Edit worker.js / public/index.html / schema.sql
# 2. If schema changed: write migrations/00X_*.sql, then:
wrangler d1 execute TMS_DB --remote --file=migrations/00X_name.sql
# 3. Deploy:
wrangler deploy
# 4. Commit + push (also triggers Cloudflare's own auto-deploy):
git add -A && git commit -m "..." && git push
```

Syntax-checking before deploy (no test runner exists in this repo):
```bash
node --check worker.js
node -e "
const fs = require('fs');
const html = fs.readFileSync('public/index.html', 'utf8');
const m = html.match(/<script>([\s\S]*)<\/script>/);
new Function(m[1]); console.log('JS syntax OK');
"
grep -oE 'id=\"[^\"]+\"' public/index.html | sort | uniq -d   # must be empty (no dup DOM ids)
```

## Roadmap (updated 2026-09-09 — supersedes any earlier roadmap mention in this file)

The originally planned "V1.2 — Requirements Intelligence / Master" is **postponed**, not cancelled — do not start it without an explicit new go-ahead. The user re-sequenced the roadmap as follows:

* V1.1A — Billing Simplification & Invoice Correction ✅ done
* V1.2 — Task Management ✅ done
* V1.3 — Live Working / Task Time Tracking ✅ done
* V1.4 — My Work / Now–Next–Later ✅ done
* V1.5 — Workflow Intelligence ✅ done
* **V1.6 — Director Control Centre ← current**
* Requirements Intelligence / Documents + LOR Management — postponed until after the core Task/Work system (V1.2–V1.4) is stable

Always trust this section's version-to-feature mapping over any older "NEXT VERSION" line found earlier in this file's history.

## Feature history (chronological, see `git log` for exact commits)

1. Initial deploy: Wrangler install, D1 database creation, schema apply, Worker deploy, GitHub push, Cloudflare Pages→Workers Git integration pivot
2. Restructured to single-Worker-serves-everything (moved `index.html` into `public/`, added `[assets]` to `wrangler.toml`)
3. Database-backed staff (replacing a hardcoded `TEAM` JS array) + insurers + manual job-ID numbering, with add/edit/delete for staff and insurers
4. Full login/session auth, `access_role` admin/staff split, `rajan` locked as permanent super admin, insurer acronyms
5. Extra job-detail fields (Policy Name/Period, Estimated/Gross Loss, Department, Appointing Office/Person, Contact Person/Phone, Address, District)

6. **TFAM Branches feature (done, migration `004_branches.sql`)**: new `branches` table (`id`, `name`, `color`, `created_at`), admin-gated CRUD (`GET/POST /api/branches`, `PATCH/DELETE /api/branches/:id`). The old single "Assign To" chip-grid on both the New Job form and the job detail side panel was replaced with **4 separate multi-select categories**: Director Responsible (staff), Surveyor Who Is Signing (staff), TFAM Branch Which Is Handling (branches), Backstaff/Surveyor Responsible (staff) — new `jobs` columns `director_ids`/`surveyor_ids`/`branch_ids`/`backstaff_ids` (JSON arrays). The legacy `assigned` column was kept (not dropped) for backward compatibility with pre-existing jobs; a new `jobStaffIds(j)` helper in the frontend unions all of `assigned` + the 3 staff-referencing category fields for display purposes (job card avatars, staff "my jobs" count) so old and new jobs both display correctly. The per-chip toggle logic was generalized into one reusable `chipsHtml(options, selectedIds)` + `toggleChip(el)` pair (driven by `data-id`/`data-color` attributes) instead of writing near-duplicate code 4 times. Branch management lives in a new "TFAM Branches" section on the Staff view, admin-gated like staff (an "+ Add Branch" button, click-to-edit chips), mirroring the insurer pattern.

7. **Admin rename + role-based assignment filtering (commit `acabd2a`)**: the "Staff" nav tab is now labeled "🛠 Admin" (UI-only — `view-staff` id, `switchView('staff')`, and the staff API/data model are all unchanged; `switchView()`'s active-state matching was made robust via a `data-view` attribute instead of parsing button text). The Director Responsible and Surveyor Who Is Signing chip selectors (both New Job form and job detail panel) are now filtered by the real `staff.role` text values (confirmed via read-only query: `"Director"`, `"Employee Surveyor"`, `"Trainee Surveyor"`, `"Backend Staff"` — no other variants exist) using new `isDirector()`/`isSurveyor()`/`isTrainee()`/`canSignSurvey()` helpers: Director Responsible → Director only; Surveyor Who Is Signing → Director + Employee Surveyor (Trainee Surveyor and Backend Staff excluded, since trainees can't sign a report); Backstaff/Surveyor Responsible and TFAM Branch Handling stay unfiltered. The job detail panel's filtered selectors union in anyone already assigned even if they wouldn't pass the filter today, so a role change never silently drops existing assignment data on save. Worker-side, `director_ids`/`surveyor_ids`/`branch_ids`/`backstaff_ids` are now validated against real `staff`/`branches` rows in both `createJob` and `updateJob` (`validateJobAssignments()`), and branch name uniqueness (case-insensitive) is a rejected error rather than silently reusing the existing row.

8. **Job Details restructure (commit `b09afa0`, migration `006_job_details_restructure.sql`)**: split into two labeled sections on both the New Job form and job detail panel — **Insurer Details** (Insurance Company, Insurer Branch Name, Appointing Office Address/District/State, Appointing Person + Email + Phone) and **Claim Details** (Insured Name/Address/District/State/Pincode, multi-contact list, Deputation Date/Mode, Loss Date, Claim No., Policy Name/No./Period, Peril, Estimated Loss, Claim Amount, Gross Loss, Department). New `jobs.contacts` JSON column (array of `{name,designation,phone,email}`) replaces the old single `contact_person`/`contact_phone` fields (kept, unused, for backward compat) — reusable helpers `contactRowHtml()`/`addContactRow()`/`renderContacts()`/`collectContacts()` drive an add/remove-row UI shared between both forms. "Deputation Date" reuses the existing `date_intimation` column (just relabeled) per the user's explicit clarification that intimation/deputation/appointment dates mean the same thing in their workflow — the separate `appointment_date`/`appointment_confirmed` pair (used for survey scheduling status) was deliberately left untouched as a distinct feature. "Deputation Mode" (Email/Call) is a new `deputation_mode` column. The old combined `appointing_office` text column is kept but no longer read/written, superseded by the split `appointing_office_address`/`_district`/`_state` columns.

## TMS V0.7 — Finalize New Job Entry (done, commit `574a67b`, migration `007_item_type.sql`)

Reorganized the New Job form (and lightly touched the job detail panel — see below) into the exact required sequence, verified live via DOM inspection:

1. **Job Title** (unlabeled, unchanged — still the only hard-required field) → **Job Number** section (relabeled from "Job ID"; blank still means auto-numbered TF-XXX, unchanged behavior)
2. **Insurer Details**: Insurance Company → Insurer Branch Name → *Appointing Office* subgroup (Address/District/State) → *Appointing Person* subgroup (Name/Email/Phone)
3. **Insured Details** (newly split out as its own top-level section, previously merged into Claim Details): Insured Name → *Insured Address* subgroup (Address/District/State/Pincode) → Contact Person(s) (multi-contact, unchanged `contactRowHtml()`/`addContactRow()`/`renderContacts()`/`collectContacts()`)
4. **Claim Details**, now organized into labeled subgroups: *Deputation* (Date, Mode) → *Loss Details* (Date of Loss, Department, Peril/Nature of Loss, **Type of Item/Property Involved** — new field, see below) → *Claim Identification* (Claim No., Policy Name, Policy No.) → *Policy Period* (From/To — reuses the existing `policy_period_from`/`policy_period_to` columns from V0.6, not renamed) → *Financial Details* (Estimated Loss, Claim Amount, Gross Loss Amount)
5. **Responsibility Allocation** (moved to be the final section before the button, now explicitly labeled — previously unlabeled): Director Responsible → Surveyor Who Is Signing → TFAM Branch Which Is Handling → Backstaff/Surveyor Responsible
6. **Create Job** button

New schema field: `jobs.item_type` ("Type of Item / Property Involved" — TEXT, default `''`), added via `migrations/007_item_type.sql`, intended to later feed a Policy Type + Department + Peril + Item Type → dynamic Document Requirement/LOR engine (not built in V0.7, deliberately). No other new columns — `policy_period_from`/`_to`, `appointing_office_*`, `contacts`, `deputation_mode`, etc. all already existed from V0.6 and were reused, not duplicated.

**Validation added** (new `isValidEmail()`, `isValidPhone()`, `validatePolicyPeriod()`, `validateContacts()` helpers, shared by New Job submit and job detail Save): Policy Period Expiry can't be earlier than Policy Period From (blocking, both client-side and server-side in `worker.js`'s `createJob`/`updateJob`); contact and Appointing Person email/phone are format-checked only when non-empty — nothing is forced to be filled in, since TFAM often deputes with incomplete information. `<input inputmode="decimal">` (not `type="number"`) was kept for the currency fields so Indian comma-formatted entry (e.g. "45,00,000") keeps working — these stay TEXT columns, not real numerics.

**Job detail panel**: deliberately NOT redesigned into the same Insured/Insurer/Claim split (per explicit instruction) — only the new `item_type` field was added into its existing Claim Details grid so it's persisted/editable there too. A full Job Update/lifecycle redesign is V0.8's job.

## TMS V0.8 — Survey Visit History (done, commit `9f590c0`, migration `008_survey_visits.sql`)

**Important note on scope**: the user's V0.8 instruction arrived as an amendment ("IMPORTANT CHANGE TO TMS V0.8 — MULTIPLE SURVEY VISITS") referencing an "original TMS V0.8 — Job Update + Survey prompt" that was never actually sent to this session — only the survey-visits amendment was received, and it was fully self-contained (data model, UI mockups, API design, validation rules, acceptance criteria all specified), so that's exactly what was built. The broader "Job Update" scope this handoff doc previously guessed at (ILA/LOR dates, FSR prep/submission, billing, etc.) was **not** addressed — those remain open, now folded into whatever V0.9 turns out to need beyond "Complete Claim Milestones" (see below).

**Core change**: survey tracking is no longer a fixed set of fields on `jobs` — a job can now have unlimited survey visits (Initial Survey → Reinspection → Joint Inspection → Dismantling Inspection → Repair Inspection → Damage Verification → Document/Record Verification → Final Inspection → Other/custom), each an independent record.

- New `survey_visits` table: `id`, `job_id` (FK), `visit_date` (required, `YYYY-MM-DD`), `visit_time` (optional, `HH:MM`), `reason` (required — preset dropdown + free-text "Other"), `inspected_by_ids` (JSON array of staff ids, required, ≥1), `remarks`, `created_by`, `created_at`, `updated_at`
- New `jobs.survey_status` (`'not_surveyed' | 'in_progress' | 'completed'`): defaults `not_surveyed`; auto-advances to `in_progress` the first time a visit is added for that job (and again if a visit is added after `completed` — treated as reopening, since new visit activity after "done" means it wasn't actually done); **never** auto-set to `completed` — only the explicit "Mark Survey Completed" button does that (`PATCH /api/jobs/:id` with `survey_status:'completed'`, reusing the existing generic job-update endpoint since it already supports arbitrary `UPDATABLE_FIELDS` + `activity_add`)
- `jobs.survey_date` (legacy single-date field) is untouched — kept for backward compatibility, no longer written by new code
- New endpoints: `GET/POST /api/jobs/:id/survey-visits`, `PATCH /api/jobs/:id/survey-visits/:visitId` (matched via regex in the routing table, placed *before* the generic `/api/jobs/:id` PATCH catch-all so they don't collide with it). Server validates job existence, staff-id existence (`validateIdsExist`, reused from the responsibility-allocation work), required date/reason/≥1 inspector, and date/time format. No DELETE endpoint (not requested — edit-with-audit-trail only, no deletion in V0.8)
- Editing a visit diffs old vs new (`updateSurveyVisit()`) and writes one activity entry per changed field (e.g. `"rajan changed Survey Visit 2 date from 2026-09-14 to 2026-09-15; inspecting staff."`); adding one writes `"{actor} added survey visit — {reason} — {date} {time}."`. Both go through a new `appendJobActivity(db, jobId, entries)` helper, since these routes are separate from `updateJob()`'s own inline activity-append logic
- New admin-only **Inspection Staff Visibility** setting (`visible`/`hidden`), backing a new generic `settings` key-value table + `GET /api/settings` (any signed-in user) + `PATCH /api/settings/:key` (admin-gated). When hidden, `listSurveyVisits()` strips `inspected_by_ids` **server-side** for non-admins (never just hidden client-side) — visit date/time/reason/remarks are always sent regardless. Toggling back to visible doesn't need to "restore" anything since the underlying rows were never touched, only omitted from the response
- `listJobs()` now does one extra query grouping all visits by `job_id` and attaches `survey_visit_count` + `last_survey_visit` `{visit_date, visit_time, reason}` (never gated — reason/date/time are never hidden) to each job in the response, so the Job Detail panel's summary needs no extra round trip
- Job Detail panel: new "Survey" section (status badge ⚪🟡🟢, visit count, last-visit line, "View Survey History" + conditional "Mark Survey Completed" buttons) — kept intentionally small, not redesigned further, per "do not overcrowd the panel." Full history/add/edit lives in its own `surveyHistoryModal` + `visitFormModal` pair, reusing the existing `chipsHtml()`/`toggleChip()` pattern for the multi-select "Persons Who Inspected" control
- All rendering/validation logic was verified via injected-fake-data DOM tests in the live browser (not real API writes) — production `jobs` row count and `survey_visits` row count were confirmed unchanged (2 and 0 respectively) before and after

## TMS V0.9 — Complete Claim Milestones (done, commit `d302e96`, migration `009_claim_milestones.sql`)

Answers "where exactly is this claim in the post-survey process?" — the full lifecycle Survey → ILA → LOR → Reminders → Documents Received → Assessment → Director Verification → Insurer Approval (conditional) → Insured Consent → FSR Preparation is now tracked, without inventing the later Document Requirement/LOR engine, FSR submission/dispatch, or billing (all explicitly deferred to later versions).

**Data model rule applied throughout**: single major milestones (ILA, LOR, Assessment, Director Verification, Insurer Approval, Insured Consent, FSR Preparation) are job-level fields; anything that repeats (Reminders, Document Receipts) is a child table with no fixed limit. 30 new `jobs` columns + 2 new tables (`claim_reminders`, `document_receipt_events`), all in `migrations/009_claim_milestones.sql`. `jobs.estimated_loss`/`gross_loss`/`claim_amount` (already existed since V0.6/V0.7) are reused for Assessment financials — only `assessed_amount` is new, per the "reuse, don't duplicate" instruction.

**Conditional Insurer Approval workflow** — the centerpiece of this version: `insurer_approval_required` is `yes`/`no`/`to_be_decided`, and the two resulting paths (with vs. without an Insurer Approval step before Insured Consent) were both verified in isolation with unit tests before deployment, specifically to confirm the "Not Required" path never gets blocked waiting on an approval that was explicitly waived. `to_be_decided` is treated as neither approved nor waived — it stays an open blocker for FSR readiness, distinctly worded ("Insurer Approval requirement still To Be Decided") from both other states.

**Server-side validation** (`validateMilestones()`, mirrors a client-side pre-check in `saveJob()` so users get instant feedback but the server is the actual authority): ILA/LOR Issue Date required when marked Issued; Director Verification Approved requires a date *and* a Verified-By who is checked against the real `staff.role` value at save time (not just filtered in the UI dropdown); Insurer Approval Approved/Partially Approved requires a date; Insured Consent Accepted requires a date; the three amount fields (`assessed_amount`, `insurer_approved_amount`, `insured_agreed_amount`) must be non-negative numbers when provided, comma-formatted Indian currency accepted. Nothing else is mandatory — TFAM often has incomplete information at any given point, matching the pattern already established in V0.7.

**Audit logging**: `saveJob()`'s `MILESTONE_FIELD_MAP` diffs all ~30 fields against the job's prior values and emits one human-readable activity line per changed field (`"{actor} changed {label} from {old} to {new}."`, with enum values translated to their display labels), rather than 30 near-duplicate `if` blocks. Two transitions get the exact spec-quoted phrasing instead of the generic template: Director Verification → Returned for Revision ("Assessment returned for revision by {name}."), and Insurer Approval Required → No ("Insurer Approval marked Not Required by {name}."). Reminders and Document Receipts log their own add/edit entries server-side (mirroring `updateSurveyVisit()`'s per-field diff pattern from V0.8) via the same `appendJobActivity()` helper, since those routes sit outside `updateJob()`.

**Job Detail panel**: the V0.8 "Survey" section is now "Claim Progress" — a compact grid showing all ten milestones at a glance (Survey/ILA/LOR/Documents/Last Reminder/Assessment/Director Verification/Insurer Approval/Insured Consent/FSR Preparation), plus buttons into the three history modals (Survey Visits, Reminders, Document Receipts — Reminders/Receipts are new, cloned from the V0.8 Survey Visit modal pattern: history list + separate add/edit form, own API endpoints). The editable milestone fields themselves (ILA & LOR, Assessment, Approvals) live in `<details>`-collapsed sections below, kept collapsed by default so the already-long panel doesn't get worse; FSR Preparation shows a live, non-blocking readiness warning (`fsrReadinessWarning()`) computed from the other milestones' current values, never hard-blocking the save.

**Verification**: no browser-testable feature in this round could be exercised via real API writes without creating production test records (explicitly forbidden), so validation logic was tested with plain Node unit tests against extracted copies of `validateMilestones()` and `fsrReadinessWarning()` (10 and 5 cases respectively, including a mocked DB for the Director-role check), and all new UI was verified via live-browser DOM tests with injected fake `jobs`/`TEAM` data (not real API calls) — screenshots confirmed the Claim Progress grid, the collapsible sections, and the Insurer-Approval-Required live field-toggle all render and behave correctly. Row counts (`jobs`=2, `claim_reminders`=0, `document_receipt_events`=0) confirmed unchanged before and after deployment.

## TMS V1.0 — FSR / Submission / Dispatch (done, commit `4642721`, migration `010_fsr_submission_dispatch.sql`)

Extends the lifecycle past FSR Preparation: Final Verification → FSR Submission → Mail Sent → Hard Copy Dispatch (conditional) → POD → Queries. **FSR submitted is explicitly not claim closed** — submission logically advances the pipeline stage to `bill_pending` (an id that already existed in the original `STAGES` list from the very first version of the app, never used until now), and Billing/Closure remain entirely out of scope for V1.1 to pick up.

**FSR Final Verification is deliberately separate from Assessment's Director Verification** (V0.9) — same shape (Pending/Approved/Returned for Revision, date, Director-only Verified By, remarks) but a distinct set of `fsr_final_verification_*` columns, since they represent different sign-offs at different points in the lifecycle. The Director-role check needed for both is now one shared `requireDirectorStaff()` helper server-side.

**The hard-copy conditional branch was the specific thing flagged for close review**: `hard_copy_required` is Yes/No/To Be Decided, and courier company, AWB/tracking number, and POD are *never* mandatory regardless of that setting — the only conditional requirement is `hard_copy_sent_date`, and only when both `hard_copy_required` and `hard_copy_sent` are `'yes'`. This was verified three ways before calling it done: (1) a Node unit test asserting the email-only path (`hard_copy_required: 'no'`) validates cleanly with every courier/POD field blank; (2) a live-browser test that opened the actual job panel with `hard_copy_required: 'no'`, called `saveJob()` for real, and confirmed zero alerts fired and the PATCH body carried through `hard_copy_required: 'no'` / `pod_status: 'not_applicable'` untouched; (3) confirmed all 9 hard-copy/POD field wrappers (`sp-hc-sent-wrap` through `sp-pod-remarks-wrap`) report `display: none` via `toggleHardCopyFields()`, not just visually tucked away. Selecting "Yes" reveals the same fields; "To Be Decided" shows its own distinct note, same three-way pattern as V0.9's Insurer Approval Required toggle.

**New `claim_queries` child table** — unlimited per job, Open/Replied/Closed, with its own `queryHistoryModal`/`queryFormModal` pair cloned directly from the Reminders pattern (V0.9) rather than designed from scratch. Query response fields (`response_date`, `response_details`) are optional even when marking Replied/Closed, per the spec's explicit "do not make the logic unnecessarily rigid."

**Stage advance is conservative and forward-only**: when `fsr_submitted` transitions to `'yes'` in a save, the client checks the job's current stage index against `bill_pending`'s index in the existing `STAGES` array — advances only if strictly earlier, never fires if the user is also manually picking a stage in that same save (that choice wins), and a job already at or past `bill_pending` (including `closed`) is left untouched. Verified with 6 scenario tests covering all of those branches, including a case at the `query` stage (chronologically before `bill_pending` but easy to mistake for "later").

**Audit logging** extends the V0.9 `MILESTONE_FIELD_MAP` diff engine with the 18 new fields, plus five more exact spec-quoted phrasings: FSR returned-for-revision, FSR submitted (with date and mode folded in), Hard Copy marked Not Required, Hard Copy dispatched (with courier/AWB folded in), and POD marked Delivered (with date). Reminders/receipts' pattern from V0.9 — server-side per-field diff via `appendJobActivity()` — is reused as-is for query add/edit.

**Job Detail panel**: Claim Progress grid now also shows Final Verification, FSR (submitted + date), Submission Mode, Mail Sent, Hard Copy, POD, and an open-query count; the three new field groups (Final Verification, FSR Submission, Hard Copy/Dispatch/POD combined into one section) are additional collapsed `<details>`, keeping the same pattern as V0.9 rather than introducing a new UI idiom for an already-long panel.

**Verification**: same constraint as V0.9 — no production test records permitted, so `validateFinalReport()` and the stage-advance function were both unit-tested standalone (7 and 6 cases respectively), and the hard-copy-bypass claim specifically was verified live against the actual deployed `saveJob()`/`buildJobPanel()` code with injected fake job data, not just logic in isolation. Row counts (`jobs`=2, `claim_queries`=0) confirmed unchanged before and after deployment; `survey_visits` is at 1 from genuine user activity between sessions, untouched by this round.

## TMS V1.1 — Billing & Closure (done, commit `c3db565`, migration `011_billing_closure.sql`)

Closes the lifecycle: Bill Required → Bill Generated → Fee Receipt(s) → Part/Full Payment → Closure Eligible → Director/Admin explicitly Closes Job. **FSR Submitted does not mean closed** — those stay two entirely separate concepts (payment status vs. job stage), enforced throughout.

**IMPORTANT ARCHITECTURAL EVENT — D1's per-table column limit was hit for the first time.** The original plan (per spec) was 8 more `jobs` columns (`bill_required`, `bill_date`, `bill_number`, `bill_amount`, `billing_remarks`, `closure_date`, `closed_by`, `closure_remarks`). `jobs` was already at 97 columns after V0.6–V1.0's additive migrations. Running the straightforward `ALTER TABLE jobs ADD COLUMN` migration against remote D1 failed outright: `too many columns on sqlite_altertab_jobs: SQLITE_ERROR`. Verified the failed attempt rolled back with zero side effects (`jobs` row count and column count both unchanged) before writing a fix.

**The fix, now the standing pattern for all future versions**: a new 1:1 sidecar table, `job_billing` (`job_id TEXT PRIMARY KEY`, FK to `jobs.id`, holding all 8 billing/closure fields + `updated_at`), instead of more `jobs` columns. Two new helpers in `worker.js` — `getJobWithBilling(db, jobId)` (fetches the jobs row + job_billing row and merges, defaulting every billing field via `JOB_BILLING_DEFAULTS` for jobs with no `job_billing` row yet) and `upsertJobBilling(db, jobId, fields)` (`INSERT ... ON CONFLICT(job_id) DO UPDATE`) — replace what would otherwise have been plain `jobs` SELECT/UPDATE calls in `updateJob()`, `createFeeReceipt()`, `updateFeeReceipt()`, and `closeJob()`. `listJobs()` fetches all `job_billing` rows once and merges per-row in memory (equivalent to a LEFT JOIN). The frontend (`index.html`) needed **zero changes** for this — it only ever consumes the already-merged JSON the API returns, so `j.bill_*`/`j.billing` reads work exactly as originally written.

**⚠️ Standing constraint for V1.2 onward: `jobs` is at 97/~100 columns and CANNOT take more `ALTER TABLE ADD COLUMN`s.** Any future version needing new single-value job-level fields (not a repeating child-table event) must create a new 1:1 sidecar table like `job_billing`, keyed by `job_id PRIMARY KEY` with an FK to `jobs.id`, and merge it in via a `getJobWith<Thing>()`/`upsert<Thing>()` helper pair following this exact pattern. Do not attempt more `jobs` columns without first running `PRAGMA table_info(jobs)` to check headroom.

**Calculated, never stored**: Total Fee Received (`SUM(fee_receipts.amount)`), Outstanding, Excess, Payment Status, and Closure Eligibility are all computed fresh on every read by `computeBilling(job, totalReceivedPaise)` — duplicated (by necessity — no shared module system between `worker.js` and `index.html`) but logic-identical in both files. All currency math is done in integer paise (`toPaise()`/`formatINR()`) to avoid floating-point bugs; rupee strings (including comma-formatted) are only ever the input/output format at the edges.

**Payment status state machine** (`to_be_decided` / `not_required` / `bill_pending` / `unpaid` / `part_payment` / `fully_paid` / `excess_received`) and **Closure Eligibility** (`financiallyEligible && fsr_submitted === 'yes'`) are two distinct outputs of the same function — a claim can be `fully_paid` yet still not closure-eligible if FSR hasn't been submitted, and this exact interaction is what Test G (below) locks in. The existing `bill_pending` stage (already in the `STAGES` list since V1.0, unused until now) persists through Part Payment and even Fully Paid — the job stage only ever becomes `closed` via the explicit Close Job action, never automatically.

**Close Job is a dedicated `POST /api/jobs/:id/close` endpoint**, not a generic stage PATCH — deliberately, so eligibility and authorization can be enforced server-side in one place a client can't bypass. `closeJob()` independently re-fetches the job via `getJobWithBilling()`, re-sums fee receipts via `sumFeeReceipts()`, re-runs `computeBilling()`, and only then checks `closureEligible` — it never trusts a client-supplied "eligible" flag. Authorization (`isAdmin(user) || isDirectorRole(user)`, where `isDirectorRole` checks the free-text `staff.role` via `/director/i`, not `access_role`) is likewise checked server-side, not just by hiding the Close Job button. `closed_by` is always `user.id` from the authenticated session — never client input.

**New `fee_receipts` child table** (unlimited per job, no fixed receipt_1/receipt_2 fields) with its own `GET/POST /api/jobs/:id/fee-receipts` and `PATCH /api/jobs/:id/fee-receipts/:receiptId` endpoints, cloned from the Reminders/Queries pattern (V0.9/V1.0). No DELETE — corrections go through PATCH, matching the "no reopening/deletion in this version" instruction.

**Audit logging**: `saveJob()`'s existing `MILESTONE_FIELD_MAP` diff engine gained the 5 non-closure billing fields (`bill_required`, `bill_date`, `bill_number`, `bill_amount`, `billing_remarks`) plus a special-cased "Bill generated —.../Bill amount changed from... to..." wording; a new `logBillingTransition(db, jobId, before, after, actor)` fires only when the *computed* payment status or closure eligibility actually changes (never on a no-op recalculation) for "Payment status became..."/"Claim became eligible for closure."/"Claim is no longer eligible for closure." lines; fee receipt add/edit and Close Job each log their own line directly.

**Verification**: same no-production-writes constraint as V0.9/V1.0. Tests A–I (unpaid, part payment, fully paid, excess, not-required, to-be-decided, FSR-gate-blocks-eligibility-despite-fully-paid, bill-changed-after-payment-reverts-to-part-payment, receipt-corrected-downward-reverts-to-unpaid) were run twice — once against the original flat-jobs-row shape during development, and again after the sidecar-table pivot against the new merged-object shape (`mergeJobBilling()` + `computeBilling()`) — all 23 assertions passed both times, confirming the pivot didn't change any actual behavior. Authorization tests (staff cannot close; director can close an eligible job; admin can close an eligible job; nobody — including admin/director — can close an ineligible job regardless of a claimed "eligible" client flag) all passed. `node --check worker.js`, the inline-JS `new Function()` syntax check on `index.html`, and the duplicate-DOM-id grep all passed cleanly. One frontend bug caught by the syntax check before deployment: the new fee-receipt-mode constant collided with V0.9's existing `RECEIPT_MODES` (document receipts) — renamed to `FEE_RECEIPT_MODES`.

**Database verification**: baseline before migration — `jobs`=2, `survey_visits`=1, `claim_reminders`=0, `document_receipt_events`=0, `claim_queries`=0, `jobs` column count=97. After the corrected migration: all five counts unchanged, `jobs` column count still 97 (untouched — the fix avoided adding columns entirely), new `job_billing`=0 rows, new `fee_receipts`=0 rows, `PRAGMA table_info` confirmed both new tables' schemas match `schema.sql`, and neither existing job (`TN-155`, `TN-156`) was marked closed.

## TMS V1.1A — Billing Simplification & Invoice Correction (done, commit `7ea0ec4`, migration `012_invoice_settlement.sql`)

A focused correction release on top of V1.1 — replaces the simplified "Bill Amount" model with TFAM's real invoice structure, adds manual TDS/Write-off tracking to fee receipts, and strips a batch of milestone fields that turned out not to be operationally needed. **The V1.1 controlled-closure architecture (dedicated `POST /api/jobs/:id/close`, server-side Admin/Director authorization, server-recomputed eligibility, `closed_by` from the session) was explicitly preserved unchanged** — this release only touches how the invoice total and settlement are calculated, not who can close a claim or how.

**UI field simplification**: removed from active forms/Job Detail/validation/audit diffing (columns kept dormant on `jobs` for backward compatibility, historical values untouched): ILA Remarks, LOR Remarks, Assessment Remarks, Insurer Approval Remarks, Insurer Approved Amount, Insured Consent Remarks, Insured Agreed Amount, FSR Submission Remarks, POD Remarks, Dispatch Remarks, and Billing Remarks (`job_billing`). Deliberately **kept**: Director Verification Remarks, FSR Final Verification Remarks, FSR Preparation Remarks, Survey Visit Remarks, Query Details/Response Details, Document Receipt details, Closure Remarks, and Fee Receipt reference info — anywhere a returned/rejected state or a financial reference genuinely needs explanation. `MILESTONE_FIELDS`/`FINAL_REPORT_FIELDS` in `worker.js` and `MILESTONE_FIELD_MAP` in `index.html` were trimmed to match; `validateMilestones()`'s amount-check loop dropped the two removed amount fields.

**New TFAM invoice structure**, replacing `job_billing.bill_amount`: `professional_fees` + `expenses` are the only manually-entered invoice inputs. Base Total, GST, and Total Invoice Value are always derived, never stored or user-editable: `Base Total = Professional Fees + Expenses`, `GST = round(Base Total × 18%)` (fixed — no rate selector, no manual GST entry), `Invoice Value = Base Total + GST`. Implemented as `computeInvoiceTotals(job)` in both `worker.js` and `index.html` (kept logic-identical, same reason as `computeBilling()` — no shared module system between the two files); the Job Detail Billing & Closure section shows Professional Fees/Expenses as inputs with Base Total/GST/Invoice Value updating live via a new `updateInvoicePreview()` on `oninput` (display-only — the server independently recalculates on every save, never trusts the client's live preview). `bill_amount`/`billing_remarks` stay on `job_billing` as dormant/legacy columns — no production billing data existed at the time of this change (`job_billing` had 0 rows), so nothing needed migrating or reinterpreting.

**Settlement now includes manual TDS and Write-off**, added to `fee_receipts` (`tds_deduction`, `writeoff_amount`, both `NOT NULL DEFAULT '0'`) alongside the existing `amount` (Received) and `reference_no` (UTR/Reference), reused as-is rather than duplicated. **TDS and Write-off are always manual entry** — TMS never calculates, assumes, or infers a TDS percentage or write-off amount from anything; the user enters the exact figure the insurer/TFAM applied. `computeBilling(job, sums)`'s signature changed from a single `totalReceivedPaise` number to `{receivedPaise, tdsPaise, writeoffPaise}` (via new `sumFeeReceiptTotals()`, replacing the old `sumFeeReceipts()`), and `Total Accounted = Received + TDS + Write-off` is compared against Total Invoice Value for Outstanding/Excess/Payment Status — the same "calculated, never stored, never trust client state" discipline as V1.1's payment engine.

**Payment status labels changed wording** (same internal enum keys, so nothing needed migrating): `fully_paid` now displays "Fully Settled", `excess_received` now displays "Excess Accounted" — matching TFAM's own settlement terminology. Receipt validation requires at least one of Received/TDS/Write-off to be greater than zero (a request with all three at zero is rejected server-side, not just discouraged in the UI) — a receipt that's purely a TDS or write-off entry with nothing physically received is valid and expected.

**Audit logging** extended: `createFeeReceipt()`/`updateFeeReceipt()` now log "Fee receipt added — Received ₹X, TDS ₹Y, Write-off ₹Z." and separate "Fee receipt corrected — Received amount changed from ₹X to ₹Y." / "TDS changed from..." / "Write-off changed from..." lines per financially-changed field (matching the spec's exact wording); the client's billing diff block now fires "Invoice created — Bill {number} — ₹{total}." on the first Professional Fees entry and "Professional Fees changed from ₹X to ₹Y." / "Expenses changed from ₹X to ₹Y." thereafter, replacing V1.1's "Bill generated"/"Bill amount changed" wording.

**Verification**: no production writes, same constraint as every prior version. Tests A–J (basic invoice, expenses-zero invoice, partial settlement, fully-settled-with-TDS, fully-settled-with-write-off, two-receipt aggregation, excess, TDS-must-not-be-calculated, zero-receipt-rejected, bill-changed-after-settlement-reverts-to-part-payment) all passed — 33/33 assertions — plus 8 additional confirmatory assertions re-checking the preserved Bill-Not-Required/To-Be-Decided/Bill-Pending/Unpaid branches, plus the 5 closure-authorization assertions re-run unchanged (staff blocked, director/admin can close an eligible job, nobody can close an ineligible one). `node --check worker.js`, the inline-JS syntax check, and the duplicate-DOM-id grep all passed cleanly; a small cleanup was needed in `toggleInsurerApprovalFields()`/`toggleHardCopyFields()` to stop referencing wrapper ids for elements removed from the DOM (harmless — `if (el)` guards meant nothing broke — but tidied for clarity).

**Database verification**: baseline before migration — `jobs`=2, `survey_visits`=1, `claim_reminders`=0, `document_receipt_events`=0, `claim_queries`=0, `job_billing`=0, `fee_receipts`=0, `jobs` column count=97. After migration: all counts unchanged, `jobs` column count still 97 (migration only extended `job_billing`/`fee_receipts`, never touched `jobs`), `PRAGMA table_info` confirmed both tables' new columns (`professional_fees`/`expenses` on `job_billing`; `tds_deduction`/`writeoff_amount` on `fee_receipts`) match `schema.sql`, and neither existing job (`TN-155`, `TN-156`) was marked closed.

## TMS V1.2 — Task Management (done, commit `1af8d71`, migration `013_task_management.sql`)

The first proper operational Task Manager: **what work has to be done, who's responsible, when's it due, and what's its status** — for both claim-linked and standalone office work. Deliberately independent of the claim/milestone system: task completion never touches claim milestones and claim milestones never touch tasks (no `Prepare ILA` task auto-marking `ila_issued`, no `Assessment – Director Approval` task auto-approving `director_verification_status`). No live timers/start-pause tracking yet — status is a plain field change in V1.2; time tracking is V1.3.

**Fully normalized, zero new `jobs` columns** — three new tables: `tasks` (job_id nullable, so a task is either claim-linked or a standalone office task), `task_assignees` (task_id/staff_id junction, `PRIMARY KEY(task_id, staff_id)` doubling as the uniqueness constraint — never comma-separated staff IDs), and `task_activity` (a dedicated audit table, since standalone tasks have no `job_id` to attach a job-scoped `jobs.activity` entry to). `jobs` stayed at 97 columns before and after — confirmed via `PRAGMA table_info` both times.

**Authoritative TFAM Task Type Master** (24 values: `job_folder_creation` through `bill_entry` plus `other`) is a controlled dropdown in both `worker.js` (`TASK_TYPES`, validated server-side) and `index.html` (mirrored, kept logic-identical to every other duplicated constant/function pair in this codebase — no shared module system between the two files). Task Type and Task Title are always separate fields.

**Full field set**: title (required), description (optional), task_type (required, controlled), priority (Low/Normal/High/Urgent, default Normal), due_date (optional), due_time (optional, only meaningful once due_date is set), expected_minutes (optional positive integer, quick-select 5/10/15/30/45/60/90/120 min or Custom in the UI), status (`not_started`/`in_progress`/`waiting`/`completed`/`cancelled`), waiting_reason (required exactly when status is Waiting, enforced server-side not just in the UI), blocked_by_task_id (optional simple dependency — display-only "Blocked" indicator, never forces a status change; rejects self-reference and non-existent tasks).

**Completion/reopen/cancellation are server-authoritative**, mirroring V1.1's closure discipline: `completed_at`/`completed_by`/`cancelled_at`/`cancelled_by` are only ever set from the authenticated session inside `updateTask()`, never trusted from the client — verified by a security test that deliberately sent a spoofed `completed_by` in the request body and confirmed the server ignored it. Reopening a Completed task (to any non-Completed status) clears `completed_at`/`completed_by` and logs "Task reopened by {user}."; there's no hard delete anywhere — Cancelled is a retained, visible state (`task_status_labels`/filterable via All Tasks), not a deletion.

**Authorization is deliberately broad, matching the rest of this codebase's philosophy**: any authenticated staff member can create a task and edit any task (mirroring `updateJob()`, which has never had a per-resource ownership restriction) — TFAM's own stated "collaborative work allocation" model, and the spec's own explicit fallback ("if existing access philosophy is broader, follow it"). The only things ever locked to the session are the identity fields (`created_by`, `assigned_by`, `completed_by`, `cancelled_by`) — never who's allowed to edit.

**Overdue/Today/Upcoming use India/office-local ("Asia/Kolkata") semantics**, computed both server-side (`computeTaskOverdue()` in `worker.js`, shifting the UTC epoch by the fixed +5:30 offset — Workers run in UTC) and mirrored client-side (`todayIST()` via `toLocaleDateString('en-CA',{timeZone:'Asia/Kolkata'})`, robust to the viewing device's own timezone). A date-only task due "today" is never overdue while it's still today — it only flips to Overdue once `due_date < today`, or (for a task with both date and time) once that specific time has passed today. `is_overdue` and `is_blocked` are attached to every task object server-side (in `listTasks()`/`getTaskWithDetails()`) and the frontend trusts them directly rather than re-deriving — consistent with the `job.billing` pattern from V1.1.

**API**: `GET/POST /api/tasks`, `GET/PUT /api/tasks/:id` (no DELETE, per spec — `PUT` was used rather than this codebase's usual `PATCH` because the spec explicitly named it). Deliberately no job-scoped `/api/jobs/:id/tasks` route or `?job_id=` filter: `GET /api/tasks` returns the full list (assignees + `is_overdue`/`is_blocked` attached, no full activity — that's fetched per-task via `GET /api/tasks/:id` only when the detail modal opens), fetched once per `fetchAll()` poll cycle exactly like `jobs`/`staff`/etc., with all filtering (assignee/status/priority/type/job/overdue/search) and grouping (My Tasks' Overdue/Today/Upcoming/No Due Date/Completed/Cancelled buckets) done client-side — the same "fetch everything, filter/group in the browser" pattern already used for the Kanban board, appropriate at TFAM's scale (~10 users).

**UI**: new "📋 Tasks" nav item (Dashboard → Tasks → Admin → Work Log) plus a global "+ Add Task" button in the topbar. The Tasks page toggles between **My Tasks** (assignee = current user, grouped into Overdue/Today/Upcoming/No Due Date sections, Completed/Cancelled hidden by default behind a "Show Completed / Cancelled" checkbox — never loaded-and-hidden, genuinely excluded from the render until asked for) and **All Tasks** (flat, filterable by assignee/status/priority/type/linked job/overdue/search across title+job number+insured name+claim number). One combined **Add Task modal** covers both the "Quick Add" and "Full Form" requirements from the spec — the always-visible fields (Title, Type, Priority, Linked Job, Due Date, Assigned To) are the quick-add set, with Description/Due Time/Expected Duration/Blocked By/Status tucked under a `<details>` "More Details" so quick-adding stays fast without needing a second, separate modal. A second **Task Detail modal** (opened by clicking any task card) is edit-in-place exactly like the Job Detail side panel — every field editable, plus contextual one-click quick-status buttons (Start/Complete/Move to Waiting/Reopen/Cancel — moving *to* Waiting focuses the mandatory reason field instead of guessing via a blind prompt), a Details block (Assigned By/Created/Updated/Completed/Blocked-by), and a full Activity History feed.

**Job Detail integration**: a new "Tasks" section (right after Claim Progress) shows Open/Overdue/Completed counts and the active (non-completed/cancelled) linked tasks, with "+ Add Task" (preselecting the current job) and "View All Tasks" (jumps to the Tasks page, All Tasks mode, pre-filtered to this job — no second Job Detail implementation; clicking a task's linked-job badge anywhere reuses the existing `openJob()`).

**Dashboard light integration**: a small, purely additive widget above the Kanban board — My Open Tasks / Due Today / Overdue, click-through to the Tasks page. No Kanban/Dashboard structural changes.

**Audit logging**: a dedicated `task_activity` table (not `jobs.activity`, since standalone tasks have no job to attach to) logs task creation (with assignees), linking/unlinking a job, title/description/type/priority/due-date/due-time/expected-duration/blocked-by changes, assignee-set changes, and the five status-transition wordings the spec called out verbatim ("Task completed by {user}.", "Task reopened by {user}.", "Task cancelled.", "Task moved to Waiting — {reason}.", "Task status changed from {X} to {Y}." for everything else) — deliberately not firing a redundant generic line on top of the special-cased ones.

**Verification**: no production writes, same constraint as every prior version. The actual `worker.js` task functions (`createTask`/`updateTask`/`listTasks`/`getTaskWithDetails`/`computeTaskOverdue`) were extracted verbatim and run against a small in-memory mock of the D1 `prepare/bind/first/all/run` surface — not a re-derived approximation. Required Tests A–R (claim-linked, standalone, multiple assignees, duplicate-assignee dedup, waiting-without/with-reason, complete, reopen, cancel, invalid assignee, invalid job, self-dependency, expected-duration positive/zero/negative, date-only-today-not-overdue, yesterday-incomplete-overdue, completed-yesterday-not-overdue, waiting-and-overdue-simultaneously, no-due-date-visible) all passed, plus one extra confirmatory test (blocked indicator clears once the blocking task completes, and never force-changes the blocked task's own status) and four security tests (spoofed `created_by`/`assigned_by`/`completed_by` all ignored in favor of the authenticated session; invalid staff IDs rejected on both create and update) — 23/23 assertions. Unauthenticated 401 on every `/api/tasks*` route is structural, not a per-function check — the task routes sit after the same `if (!user) return error('Not authenticated', 401)` gate as every other endpoint in `worker.js`. `node --check worker.js`, the inline-JS syntax check, and the duplicate-DOM-id grep all passed cleanly.

**Database verification**: baseline before migration — `jobs`=2, `survey_visits`=1, `claim_reminders`=0, `document_receipt_events`=0, `claim_queries`=0, `job_billing`=0, `fee_receipts`=0, `staff`=10, `jobs` column count=97. After migration: all eight counts unchanged, `jobs` column count still 97, new `tasks`/`task_assignees`/`task_activity` all at 0 rows, `PRAGMA table_info` confirmed all three new tables' schemas match `schema.sql`, and neither existing job (`TN-155`, `TN-156`) was marked closed.

**Known limitation**: this session has no login credentials for the live app, so the post-deploy smoke test was limited to confirming the login page renders cleanly with no console errors (as with every prior version) — the authenticated flows (Dashboard, Tasks page, Job Detail Tasks section, Add/Edit Task modals) were not exercised live in a browser, only via the isolated Node test suite against the actual extracted logic. Recommend the user do a quick manual pass after review.

## TMS V1.3 — Live Working / Task Time Tracking (done, commit `b82f78b`, migration `014_task_work_sessions.sql`)

Answers what V1.2 deliberately left open: **who is actually working on what right now, since when, and how much time did a task actually take.** The core invariant driving this entire version: **one active working task per staff member** — a staff member can be *assigned* many tasks, but can only be *actively timing* one at a time. No timer field lives on `tasks` itself; everything (active worker, actual time, live elapsed time) is derived from a new normalized `task_work_sessions` table, exactly as instructed.

**`task_work_sessions` schema**: `id`, `task_id`, `staff_id`, `started_at`, `ended_at` (nullable — `NULL` means this row *is* the staff member's one active session), `duration_seconds` (nullable, computed once at close time from `ended_at - started_at`, never continuously written), `end_reason` (`paused`/`switched_task`/`task_completed`/`cancelled`/`assignment_removed`/`manual_correction`), `created_at`/`updated_at`, `adjusted_by`/`adjustment_reason` (Admin/Director correction metadata). No columns were added to `jobs` or `tasks` — `jobs` stayed at 97 columns before and after, confirmed via `PRAGMA table_info` both times.

**One-active-session enforcement is two layers, exactly as specified**: (1) application-level check-then-act in `startOrResumeWork()` — before opening a session it looks for the staff member's existing open session and, if one exists on a *different* task, closes it with `end_reason='switched_task'` first; (2) a **database-level partial UNIQUE INDEX** — `CREATE UNIQUE INDEX idx_task_work_sessions_one_active_per_staff ON task_work_sessions (staff_id) WHERE ended_at IS NULL` — confirmed to apply cleanly to D1 (unlike the `jobs`-column-count wall hit in V1.1, this is standard SQLite partial-index syntax and D1 supports it without issue). The index is the real backstop under a request-level race; the application logic is the friendlier-error-message primary layer. A duplicate-insert attempt is caught and surfaced as "You already have an active work session — please refresh and try again," never a raw SQL error.

**Start and Resume are literally the same server operation** (`startOrResumeWork()`), matching the spec's explicit instruction — the only difference is what state the task/session happened to be in beforehand. Starting a `not_started` task moves it to `in_progress`; starting a `waiting` task is treated as an explicit Resume (moves to `in_progress`, clears `waiting_reason`); starting a task the user is already actively timing is a no-op returning the existing session (no duplicate row). Only an assigned staff member may start/resume their own work — the function takes no `staff_id` parameter at all, only the authenticated `user`, so there is nothing to spoof.

**Pause** closes the caller's own active session on the named task only (`end_reason='paused'`) — a user can never pause anyone else's timer through this route (Admin/Director corrections are the separate, distinct mechanism below). **Task status and live-work status are fully decoupled**, per the spec's core distinction: Pause never reverts task status away from `in_progress` — "In Progress + no active session" is an explicitly valid state.

**Completed/Cancelled/Waiting are the three states that must never coexist with an open session** — this is enforced in one place, inside the existing `updateTask()` (V1.2), not a separate code path: whenever a status transition lands on `completed`, `cancelled`, or `waiting`, `closeAllActiveSessionsForTask()` closes *every* staff member's open session on that task (not just the actor's) with the matching reason (`task_completed`/`cancelled`/`paused` respectively) before the rest of `updateTask()`'s existing V1.2 logic runs. This means completing a task while multiple people are actively timing it closes all of their sessions in the same request, and completing a task nobody was timing creates no fake session — both paths already shared the one function, so no duplication was needed. Reopening a Completed task (still via `updateTask()`, unchanged from V1.2) preserves all historical session rows and never auto-starts a new one — the user must explicitly Start/Resume.

**Removing an assignee closes their active session** (`end_reason='assignment_removed'`) — added to `updateTask()`'s existing assignee-diff block: for each staff member being removed, if they have an open session on that task, it's closed before the reassignment is written, so nobody is ever left "actively working" on a task they're no longer assigned to.

**Manual correction and force-stop are the same endpoint** (`PUT /api/work-sessions/:id`, Admin/Director only, mandatory `adjustment_reason`): correcting a completed session's `started_at`/`ended_at` recalculates `duration_seconds` and sets `end_reason='manual_correction'`; supplying an `ended_at` on a still-open session *is* the "Stop Session" action the spec asks for — same code path, same audit discipline, no separate mechanism needed. Sessions are never hard-deleted, only corrected in place with `adjusted_by`/`adjustment_reason` always recorded and a "Work session corrected by {user} — {reason}." activity line logged.

**Actual time**: Total Actual Effort = `SUM(duration_seconds)` of closed sessions on a task, **plus** the live-derived elapsed time of any still-open session for display purposes only (never written to D1) — shown per-task in Task Detail's new "Work Sessions / Work History" table, alongside Expected vs. Actual (a plain `+27 min` / `-18 min` difference, deliberately no "Efficient/Inefficient" judgment language anywhere) and per-staff subtotals when more than one person has worked the task.

**Global Active Work Bar**: a compact strip between the topbar and the stats strip — outside every `.view` container, so it survives `switchView()` calls automatically without any extra wiring — shows the current user's active task, a client-side `HH:MM:SS` ticker (one persistent 1-second `setInterval`, started once at login, not restarted per render), and Pause/Open Task buttons. **My Tasks** cards show "WORKING NOW" with the same ticking timer for the current user's own active task, a neutral "In Progress · Not currently active" for an untimed in-progress task, and a static (poll-refreshed, not per-second) "{name} working · {n} min" when someone else is actively timing a task visible to the viewer — deliberately never "Idle" anywhere, this is task-work tracking, not attendance or surveillance, per the spec's explicit instruction.

**Live Team** is a third mode on the Tasks page (My Tasks / All Tasks / **Live Team**) rather than a separate Director-only screen, listing every currently-open session across all staff with Staff/Task/Job/Started/Current Duration/Expected — clicking a row opens that task's detail via the existing `openTaskDetail()`. **Made visible to all authenticated staff, not gated to Admin/Director** — a deliberate call following the spec's own "prefer existing transparency model" guidance: nothing else in this codebase restricts visibility of who's working on what (job assignments, milestone status, and now tasks are all fully visible to every signed-in staff member), so Live Team follows that established pattern; Admin/Director naturally retain full visibility as a subset of "all staff." Sessions open longer than 8 hours are flagged "Long-running session" — visibly, never auto-corrected; the human decides.

**Work Log integration**: a new, purely additive "Task Work" subsection below the existing job-activity log (which is completely untouched) — its own toolbar (staff/job/task-type/date filters) and list, backed by a new `GET /api/work/sessions` endpoint (raw rows, capped at 500 most-recent, mirroring `listLog()`'s exact convention) enriched client-side from the already-loaded `tasks`/`jobs`/`TEAM` arrays, same pattern as the log above. A **Daily Work Summary** bar (Today Tracked / Sessions / Tasks Worked) appears above the task list in My Tasks mode, computed from the same session data filtered to the current user and today (IST).

**API**: `POST /api/tasks/:id/start`, `POST /api/tasks/:id/pause`, `GET /api/tasks/:id/work-sessions`, `GET /api/work/live` (Live Team), `GET /api/work/sessions` (Work Log/Daily Summary, capped 500), `PUT /api/work-sessions/:id` (Admin/Director correction/force-stop). `GET /api/work/current` also exists per spec but the web client derives "my current session" from the already-fetched `/api/work/live` list instead of an extra round trip — kept the endpoint for API completeness (e.g. a future thinner client) rather than removing it.

**Verification**: no production writes — the actual `worker.js` functions (`startOrResumeWork`/`pauseWork`/`correctWorkSession`/`closeAllActiveSessionsForTask`/`updateTask`/etc.) were extracted verbatim and run against an in-memory mock of the D1 surface, including a simulated partial-unique-index rejection to prove the concurrency backstop. Required functional Tests A–X (start-first-task, start-same-task-twice-no-dup, switch-tasks, manual-pause, resume-creates-new-row, complete-while-active, complete-without-active-session, multi-assignee-simultaneous, one-staff-cannot-double-active, completed/cancelled-task-start-rejected, waiting-task-resume, waiting-while-active-closes-sessions, cancelled-while-active-closes-sessions, multi-session-total, multi-staff-effort, expected-vs-actual, active-session-elapsed-no-db-write, reopen-preserves-history, remove-active-assignee-closes-session, manual-correction, non-admin-correction-rejected, current-work-lookup, live-team-excludes-completed) all passed, plus security tests (cannot start as another staff member, cannot pause another person's session, spoofed timestamps/staff_id impossible to supply in the first place since the functions take no such parameters, correction requires a reason, completed/cancelled tasks reject Start, one-active-session holds under a simulated concurrent double-insert) and DB invariant checks (no staff with >1 open session; no Completed/Cancelled/Waiting task with an open session) — 33/33 assertions. Unauthenticated 401 is structural, confirmed by route placement after the same auth gate as every other endpoint. `node --check worker.js`, the inline-JS syntax check, and the duplicate-DOM-id grep all passed cleanly.

**Database verification**: baseline before migration reflected genuine production activity since V1.2 shipped — `jobs`=2, `survey_visits`=2 (was 1), `job_billing`=2 (was 0), `tasks`=1, `task_assignees`=1, `task_activity`=1 (all were 0 at V1.2 completion — real staff have started using the Task Manager), `staff`=10, `jobs` column count=97. After migration: all counts unchanged, `jobs` column count still 97, new `task_work_sessions`=0 rows with zero active sessions, the partial UNIQUE INDEX confirmed present via `sqlite_master`, and neither existing job was marked closed. No fake session or task data was created at any point.

**Known limitation**: same as every prior version — this session has no login credentials for the live app, so the smoke test was limited to confirming the login page renders cleanly with no console errors post-deploy. The authenticated flows (Start/Pause/Resume, the Active Work Bar, Live Team, Work History correction UI, Work Log's Task Work subsection) were verified via the isolated Node test suite against the real extracted logic and by static review, not exercised live in a browser. Recommend a manual pass, especially clicking Start on a real (or throwaway-then-cancelled, never faked) task to visually confirm the ticking timer and the switch-task confirmation dialog.

## TMS V1.4 — My Work / Now–Next–Later (done, commit `5dee834`, migration `015_my_work_preferences.sql`)

Answers what V1.2/V1.3 deliberately left open: **what should I actually work on right now?** A deterministic, rules-based recommendation engine (explicitly not AI — no external API calls anywhere) turns each staff member's full assigned-task list into a personal NOW/NEXT/LATER queue. **The recommendation engine is advisory only** — nothing here ever mutates `tasks`, `jobs`, `task_work_sessions`, or claim milestones; `GET /api/my-work` is purely read-only (confirmed via source inspection — zero `.run()` calls in `computeMyWork()` — and via live verification against real production data with row counts identical before/after).

**No new `tasks`/`jobs` columns and no stored score** — `jobs` stayed at 97 columns. The only new schema is `task_personal_preferences` (`staff_id` PRIMARY KEY, `next_task_id`, `updated_at`) for the one-per-user "Do Next" pin — the PK itself enforces "at most one pin per staff member," same pattern as V1.3's partial-unique-index for one-active-session. Everything else (rankings, eligibility, NOW/NEXT/LATER, Waiting/Blocked, closed-claim exceptions) is computed fresh on every request from existing `tasks`/`task_assignees`/`task_work_sessions`/`jobs` data — correctness over caching, appropriate at TFAM's scale.

**Ranking engine** (`scoreTaskForUser(task, ctx)` in `worker.js`) is a pure, deterministic, unit-tested function — given the same task and context it always returns the same score and reason codes. Scoring dimensions, exactly per spec: Overdue (+100, +5/day aging capped at +50), Due Today (+80) with due-time urgency layered on top (within 1hr +40, within 3hr +25, later today +10), Due Tomorrow (+50), Due in 2–3 days (+30), Priority (Urgent +80/High +50/Normal +20/Low +0), Task Age (+1/day capped at +30), Old-Quick-Task escalation (expected_minutes ≤15, +15 at 7+ days / +30 at 14+ days / +50 at 30+ days pending — naturally never applies to Waiting/Blocked tasks since those are filtered out before scoring, not by a special-case check), In-Progress continuity (+20). **Stored task priority is never touched** — the score is purely derived, read-only intelligence layered on top of the human-entered field.

**Reason codes, not raw scores, cross the API boundary** — `worker.js` returns structured `{code, days/names}` objects (`overdue`, `due_today`, `urgent`, `old_task`, `quick_task_old`, `previously_started`, `fits_time`, `quick_win`, `pinned_next`, `needs_more_time`, `someone_else_working`, etc.); `index.html`'s `REASON_CODE_TEXT` map turns those into the friendly one-line explanations shown on every NOW/NEXT card (e.g. "Overdue 3 days · High Priority"). No raw "Score 175" ever reaches the UI.

**Eligibility**: a task is actionable (appears in NOW/NEXT/LATER) only when the user is an assignee, status is Not Started or In Progress, and it's neither Completed, Cancelled, Waiting, nor blocked by an incomplete dependency (`is_blocked`, reused directly from V1.2/V1.3's existing computation — no new logic needed, and a completed blocker automatically un-blocks on the very next request with no manual "unblock" flag). Waiting and Blocked tasks are mutually-exclusive-with-actionable and shown in their own section instead, never both places. "Waiting since" is derived from the task's own `task_activity` log (the most recent "Task moved to Waiting —" entry's timestamp) rather than a fabricated field — if that entry doesn't exist for some reason, no date is shown, just the reason text.

**Closed-claim handling** (spec item 45, resolved by actually inspecting `closeJob()`): in this system a job can only reach `stage='closed'` via the explicit Admin/Director-gated close action, which itself requires FSR submitted **and** billing fully settled — so Closed genuinely only happens post-billing/payment, exactly the condition the spec said would make an open task on a closed claim "exceptional." Open tasks on a closed job are therefore excluded from normal recommendations and surfaced separately as "Linked claim is closed" — **except** `bill_entry` and `entry_updation` task types, which are legitimate real post-closure housekeeping work and remain fully actionable.

**Current active task has absolute priority** — if the user has an open V1.3 work session, that task always owns NOW regardless of score (`recommended_now` is explicitly `null` whenever `current` is set), is never duplicated into NEXT/LATER, and Pause/Complete act on it directly through the existing V1.3 endpoints. With no active session, the single top-scored actionable task becomes "RECOMMENDED NOW" and is *excluded* from the NEXT list below it (so NEXT always shows the next 3–5 *different* tasks, never a repeat of the featured one).

**"I Have Time" / available-time ranking**: `available_minutes` is a plain optional query param on `GET /api/my-work` (validated server-side as a positive integer, rejecting zero/negative/non-integer). A fitting task (`expected_minutes <= available`) gets +30, plus +10 more if it's ≤75% of the window — additive on top of urgency, never a hard filter, so an urgent overdue 60-minute task is never hidden even under a 15-minute window (it just won't win the RECOMMENDED NOW slot unless nothing else fits, in which case it's flagged `needs_more_time` rather than silently vanishing). Unknown-duration tasks get no suitability bonus, which — being purely additive scoring, not a forced sort partition — naturally ranks them below an otherwise-equal known-fitting task without any special-case sort logic. The selection is **pure client-side UI state** (`sessionStorage`, cleared when the tab closes) — nothing is written to D1, and it resets to no-filter on next login by design.

**Do Next pin**: `PUT /api/my-work/pin` (`{task_id}` to set, `{task_id: null}` to clear), restricted to tasks the caller is actually assigned to, storing only under that user's own `staff_id` — completely invisible to and non-effecting of any other assignee's ranking on the same shared task. A pinned task gets a dominant +500 score bonus (wins NOW over anything else eligible) but is still subject to the same eligibility gate as everything else: if a pinned task becomes Waiting/Blocked/Completed/Cancelled, it simply stops appearing as executable and the pin has no effect until/unless it becomes eligible again.

**Multi-assignee behavior**: the same task can be recommended to multiple assignees independently (each person's pin, active session, and score context are fully personal). If a shared task already has another assignee actively working it, the viewer sees a `someone_else_working` reason note (never hidden, never blocked from starting it themselves) with a small deprioritization (−10) unless the task is urgent or overdue, so genuine urgency is never suppressed by someone else's presence.

**UI**: new "🎯 My Work" top-level nav item (Dashboard → My Work → Tasks → Admin → Work Log — Tasks is untouched, still the full operational table). The My Work page is deliberately much lighter than Tasks: a personal Daily Summary bar (Open/Today/Overdue/Waiting/Tracked Today — the last one reusing V1.3's exact session-duration math), an "I Have Time" pill row, one dominant NOW card (working-now ticking timer reuses V1.3's existing `.task-live-timer`/`startTaskCardTimers()` ticker — no new interval needed), a NEXT list of compact ranked cards with one-line reasons, a compact LATER list with a "Show all" toggle instead of a second full table, and a Waiting/Blocked section. A small, purely additive Dashboard widget (Working Now / Next / Overdue, click-through) was added above the existing Kanban — no Dashboard redesign.

**Verification**: no production writes anywhere in this version. The actual `worker.js` functions (`scoreTaskForUser`/`compareScored`/`computeMyWork`/`setDoNextPin`, plus everything from V1.2/V1.3 they depend on) were extracted verbatim and run against an isolated mock-D1 harness using **fixed timestamps** (per spec) so results never depend on wall-clock date. Required ranking Tests A–V all passed (38/38 assertions, including a stable deterministic tie-breaker test and the specific "urgent long task remains visible under a 15-min window while a fitting quick task still wins the NOW slot" scenario), plus "I Have Time" window-parsing tests (5–120 min) and zero/negative/non-integer rejection, plus security tests (`computeMyWork`/`setDoNextPin` take no staff_id parameter to spoof — identity only ever comes from the authenticated session). Several early test failures during development turned out to be test-assertion bugs (not checking `recommended_now` alongside `next`/`later` when only 1–2 tasks existed), not actual defects — corrected and re-verified. `/api/my-work` and `/api/my-work/pin` both sit after the same shared auth gate as every other endpoint (structural 401). **Additionally verified live against real production data**: this session's browser happened to already hold an authenticated cookie from earlier testing, so — read-only only, zero writes — the actual My Work page, Dashboard widget, "I Have Time" filter, Tasks page (My Tasks/All Tasks/Live Team), and a real Task Detail's Work History table were all exercised against TFAM's genuine live task/session data (a real 12-second tracked session, correctly totaled) with zero console errors; row counts confirmed byte-identical before and after. This is a stronger confirmation than the login-page-only check every prior version was limited to.

**Bug found during this verification, not fixed here (flagged separately)**: `#td-waiting-reason-wrap` and `#taskFilters` (and likely other elements toggled via `el.hidden = ...`) stay visually visible despite `hidden` being set, because their own CSS class declares `display:flex` and — per normal CSS cascade rules — author-stylesheet rules always beat the browser's default `[hidden]{display:none}` UA rule, regardless of specificity. Confirmed via direct `getComputedStyle` inspection on the live app. This bug was introduced in V1.2 (`toggleTaskWaitingReason`, `taskFilters` toggling) and reused in V1.3/V1.4 without being caught until this session's live verification. A follow-up task has been queued (see the task chip / ask for "Fix hidden-attribute CSS bug across Tasks/My Work UI") to audit every `.hidden = ` toggle site and add a proper `[hidden]{display:none!important}` rule; it's a frontend-only CSS fix with no migration or production-data risk, deliberately not folded into this version to avoid further scope growth on an already-large release.

**Database verification**: baseline before migration reflected further genuine production activity since V1.3 — `jobs`=2, `survey_visits`=2, `document_receipt_events`=1 (was 0), `job_billing`=2, `tasks`=1, `task_assignees`=1, `task_activity`=5 (was 1), `task_work_sessions`=1 (was 0 — a real Start/Pause cycle happened, confirmed closed with `end_reason='paused'`, no active session), `staff`=10, `jobs` column count=97. After migration: all counts unchanged, `jobs` column count still 97, new `task_personal_preferences`=0 rows, and neither existing job was marked closed. No fake task/session/preference data was created at any point — every real row seen above was created by genuine TFAM staff usage between sessions, not by this work.

## TMS V1.5 — Workflow Intelligence (done, commit `37082f1`, no migration)

Answers what V1.2–V1.4 deliberately left open: **where is each claim in the TFAM workflow, what should normally happen next, and what's holding it up?** Entirely rule-based (no AI/LLM call anywhere) and strictly **observational + advisory** — it derives a phase/current-step/next-action/attention/health analysis from existing data and shows it to staff; it never writes `jobs.stage`, any milestone field, creates/completes a task, or bypasses the existing Director/Admin-only approval and closure gates. Confirmed via source inspection (zero `.run()` calls anywhere in the workflow module) and via live verification against real production data (row counts identical before/after extensive interactive testing).

**Regression fix done first, as instructed**: the `hidden`-attribute CSS bug flagged during V1.4 (elements toggled via `el.hidden = ...` staying visually visible because their own CSS class set `display:flex`, which — per normal CSS cascade rules — beats the browser's UA-stylesheet `[hidden]{display:none}` regardless of specificity) is fixed with one global `[hidden]{display:none!important}` rule near the top of the stylesheet. The five pre-existing narrower per-component `[hidden]` overrides (`.modal-overlay[hidden]`, `.login-overlay[hidden]`, etc.) were left in place as harmless now-redundant duplicates rather than churning unrelated lines. Verified live on the deployed app via direct `getComputedStyle` inspection: Task Detail's Waiting Reason field is `display:none` for In Progress, `display:flex` for Waiting, and `display:none` again after switching back to In Progress; the Tasks page's filter row is `display:none` in My Tasks mode and `display:flex` in All Tasks mode. No production data was touched by this check (confirmed via a direct read-only query of the one real task's `status`/`waiting_reason` immediately after).

**No migration** — a deliberate, spec-preferred outcome, not an oversight. Every derived value (phase, current step, next action, attention flags, health, days-since-activity) is computed fresh on every request from `jobs` + `job_billing` + `fee_receipts` + `survey_visits` + `claim_reminders` + `document_receipt_events` + `claim_queries` + `tasks`/`task_assignees`/`task_activity`/`task_work_sessions` — no new table, no cache column on `jobs` (still exactly 97), no `workflow_status_cache`/`job_current_step` table. `schema.sql` was not touched at all this version.

**Workflow model**: an ordered `WORKFLOW_STEPS` array in `worker.js` (appointment → inspection → ILA → LOR → document collection → assessment → director verification → insurer approval → insured consent → FSR preparation → FSR final verification → FSR submission → dispatch → POD → billing → closure), each entry grounded in a real, checkable field — **not** the conceptual TFAM sequence verbatim. "Upload Field Notes/Documents" (conceptual step 5) has no backing `jobs` column, so it is deliberately not modeled as a gating step (it remains a selectable task type only). "Document Collection" has no explicit "all documents received" milestone field either — per spec's explicit caution against inventing one, its completion signal is `assessment_status` leaving `'not_started'`, the real staff-driven signal that documents were sufficient to begin assessment.

**Conditional workflow respected throughout**: each step's `applicable(job)` returns `'yes'`/`'no'`/`'to_be_decided'` from the corresponding real `*_required` field (ILA/LOR/Insurer Approval/Hard Copy/Bill Required) — `'no'` silently skips the step (shown as N/A in the timeline, never flagged as a problem); `'to_be_decided'` blocks progress **and** becomes its own `decide_required` attention item, never guessed as either yes or no. POD inherits Hard Copy's own applicability rather than a separate check, since the step walk never reaches it until Dispatch itself resolves.

**Rework handling**: `director_verification_status`/`fsr_final_verification_status` = `'returned_for_revision'` overrides the next-action label to "Rework assessment"/"Rework FSR" (surfacing the actual remarks field when present) without needing a separate step in the model — the normal walk already lands on the right step id, only the label/reason changes.

**Billing/closure phase reuses V1.1A/V1.1's exact existing functions** (`computeBilling()`, `getJobWithBilling()`, `sumFeeReceiptTotals()`) — **not a second closure policy**. Once every sequential step is complete, the phase becomes `billing_closure` and branches purely on `billing.billRequired`/`billing.paymentStatus`/`billing.closureEligible`, exactly the same fields `closeJob()` itself checks. "Ready for Closure" only ever appears when `billing.closureEligible` is true, and the Job Detail card's Close Job button reuses the existing `openCloseJobModal()` — still Admin/Director-gated, still requires the explicit confirmation, still server-recomputes eligibility independently inside `closeJob()` itself (workflow intelligence never becomes a second source of truth for who may close what).

**Days-since-activity / stall detection**: `getLastMeaningfulActivity()` takes the max of real timestamps only — `jobs.updated_at`, every linked survey visit/reminder/document-receipt/query's `created_at`/`updated_at`, and every linked task's `task_activity`/`task_work_sessions` timestamps — never a fabricated date for a field that lacks one, falling back to `jobs.created_at` when nothing else exists. Thresholds are named constants (`STALL_THRESHOLDS_DAYS = {watch:4, attention:8, stalled:15}`), not scattered magic numbers. `workflow_health` combines this aging signal with a floor: an `overdue_task`/`query_open`/`decide_required` attention flag forces at least `'attention'` even on a recently-active claim, exactly matching the spec's "Attention: overdue task / pending approval / missing follow-up" description (not purely an aging function). Closed claims are never flagged regardless of age.

**Attention flags** use structured `{code, ...data}` objects (`decide_required`, `reminder_due`, `overdue_task`, `no_matching_task`, `query_open`, `bill_pending`, `payment_pending`, `task_milestone_mismatch`, `workflow_inconsistency`) — the frontend's `WORKFLOW_ALERT_TEXT` map turns codes into friendly one-line text, never raw scores. Reminder-due respects the claim's own configured `reminder_frequency`/`reminder_frequency_custom_days` (including `'custom'`), comparing against the actual last `claim_reminders` timestamp — never an automatic reminder send, purely a "this is due" flag.

**Task/milestone mismatch and data-inconsistency detection are deliberately modest** — a handful of high-confidence checks (assessment-prep task completed vs. milestone still Not Started and the reverse; FSR Print & Mail / Report Dispatch tasks completed vs. the corresponding milestone still pending; FSR Submitted=Yes vs. Preparation Not Started; POD Delivered vs. Hard Copy not Sent; Insured Consent Accepted vs. Assessment not Prepared; Closed stage with no closure record; stage disagreeing with an already-submitted FSR) — not an exhaustive validation engine, per the spec's explicit caution against one. None of these ever mutate either the task or the milestone; they're advisory-only.

**No task creation is ever automatic**: `no_matching_task` is purely a flag. "+ Add Suggested Task" opens the *existing* V1.2 Add Task modal (`openTaskForm()`, extended with an optional `prefill` parameter) with job/type/title pre-filled — the user still reviews and explicitly clicks Add Task. `next_action.task_type` maps 1:1 to the authoritative TFAM Task Type Master (`assessment_preparation`, `assessment_director_approval`, `insurer_approval`, `insured_approval`, `fsr_preparation`, `fsr_review`, `report_dispatch`, `courier_tracking`, `bill_entry` — verified via dedicated mapping tests), never a duplicated/invented type.

**API**: `GET /api/jobs/:id/workflow` (single job, for the Job Detail card) and `GET /api/workflow/attention` (every open job, for the Dashboard — bulk-fetches all child tables once via `buildWorkflowContexts()`, mirroring `listJobs()`'s established "fetch once, group by job_id" pattern rather than N+1 per-job queries). Both sit after the same shared auth gate as every other endpoint (structural 401); neither accepts any parameter that could spoof identity or trigger a write.

**UI**: a compact "WORKFLOW" card near the top of Job Detail (phase, current step, health badge, next action, reason, activity recency, attention lines, conditional Add-Suggested-Task/Close-Job buttons) fetched asynchronously via `loadJobWorkflow()` right after the panel opens (doesn't block the existing synchronous panel render), plus a compact timeline strip (✓ done / ● current / ○ pending / N/A skipped) below it. A new "Claims Needing Attention" section on the Dashboard (`GET /api/workflow/attention`, sorted by the spec's exact severity order — stalled > overdue task > open query > approval-pending step > reminder due > no matching task > watch, older inactivity first within a tier) shows up to 10 rows with a primary + secondary attention line each (never all flags at once, avoiding the flooding the spec warned against), clicking through to the existing `openJob()`.

**Bug found and fixed during live verification**: the first version of "+ Add Suggested Task" embedded a raw `JSON.stringify(...)`'d title directly inside a double-quoted `onclick="..."` HTML attribute — the JSON string's own double quotes prematurely closed the attribute, corrupting the handler and throwing `Uncaught SyntaxError: Unexpected end of input` on click. Fixed by wrapping it in `esc(...)` (HTML-entity-encoding the quotes) before embedding; re-verified live against the real deployed app with the fix — the Add Task modal now opens correctly prefilled (confirmed: Title "ILA — TN-155", Type "Prepare ILA", Linked Job "TN-155 — ...", Assigned To defaulting to self) with no console error on that specific action.

**Verification**: no production writes anywhere in this version (confirmed both by source inspection and live testing). The actual `worker.js` functions (`analyzeJobWorkflow`/`deriveWorkflowSteps`/`findWorkflowAlerts`/`deriveWorkflowHealth`/`getLastMeaningfulActivity`/`findTaskMilestoneMismatches`/`findWorkflowInconsistencies`, plus `computeBilling` reused unchanged) were extracted verbatim and tested with **fixed timestamps** (per spec) against synthetic fixtures — no DB needed for `analyzeJobWorkflow()` itself since it's a pure function taking a plain job + precomputed context. Required Tests A–Z all passed (55/55 assertions total, including conditional-branch coverage for every To-Be-Decided gate, all four stall thresholds plus "newer activity resets aging" and "closed claim never flagged", 8 task-type-mapping assertions, no-task-alert tests including "a completed old task alone doesn't satisfy new pending work", task/milestone mismatch tests in both directions confirming no mutation occurs, and data-inconsistency tests including a "valid conditional state must never false-positive" check). Security is structural (route placement after the shared auth gate; no spoofable parameters exist on either endpoint). `node --check worker.js`, the inline-JS syntax check, and the duplicate-DOM-id grep all passed cleanly.

**Live production read-only validation** (this session's browser held an already-authenticated cookie, as in V1.4): the actual Dashboard Claims Needing Attention list, a real Job Detail's Workflow card + timeline, Add Suggested Task's prefill (after the fix), Live Team, and Work Log were all exercised against TFAM's genuine live data with zero functional defects found beyond the one fixed above. Row counts confirmed byte-identical before and after.

**Known limitation — unresolved, flagged separately**: every page load of the deployed app logs one uncaught `SyntaxError: Unexpected end of input` with no stack trace/file/line available from the tooling used to observe it. Substantial investigation (deployed-source-vs-local diff, `new Function()` parse check against both, `document.scripts` count, manual `JSON.parse` of every new API response, direct calls to new render functions, origin-isolation check against a different site) found **no functional impact and no confirmed source** — every actual feature works correctly. This was not present during V1.4's equivalent live-verification pass in the same session. A follow-up task has been queued (see the task chip: "Investigate mysterious 'Unexpected end of input' console error on TMS load") recommending real Chrome DevTools with "Pause on exceptions" for whoever picks it up, since the tooling available this session couldn't capture a throw-site stack trace.

**Database verification**: baseline before this version — `jobs`=2, `staff`=10, `survey_visits`=2, `document_receipt_events`=1, `job_billing`=2, `tasks`=1, `task_assignees`=1, `task_activity`=5, `task_work_sessions`=1, `task_personal_preferences`=0, `jobs` column count=97. No migration was applied (none needed), and all counts were re-confirmed identical after extensive live interactive testing — including the explicit CSS-regression check that directly manipulated (client-side only, never saved) a real task's status field.

## Not yet built / open items

The Document Requirement Master / dynamic LOR engine ("Requirements Intelligence / Master") is **postponed**, not cancelled — see the Roadmap section above; do not start it without an explicit new go-ahead. Director Control Centre is the immediate roadmap (V1.6, see Roadmap section). Automatic reminder sending, email automation, invoice PDF generation, and accounting/GST integration remain out of scope, no target version yet. Reopening/editing a closed job remains explicitly deferred. Explicitly NOT built in V1.5, reserved for later: cross-staff/Director-level workload aggregation (Director Control Centre is its own future version), any automatic task creation or milestone/stage mutation from workflow analysis (permanently out of scope for this feature, not just deferred — it is observational by design), AI/LLM-based reasoning of any kind. Attendance/surveillance features remain permanently out of scope. **Known pending items** (queued, not yet applied): the "Unexpected end of input" console error described above.

**NEXT VERSION: TMS V1.6 — DIRECTOR CONTROL CENTRE**

## Known minor items not acted on

- `DEPLOY_1.md` is an exact duplicate of `DEPLOY.md` sitting in the repo root, untracked by git. Never resolved — user hasn't said whether to delete it or keep it.
